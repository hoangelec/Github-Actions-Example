name: iOS Example workflow

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Test bundler for fastlane and run SwiftLint
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bundle install  
        run: |
          bundle install
      - name: Install mint
        run: |
          brew install mint
          mint bootstrap
          mint run swiftlint
